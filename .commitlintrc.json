{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "scope-enum": [
      2,
      "always",
      [
        "api",
        "web",
        "db",
        "shared",
        "config",
        "auth",
        "projects",
        "finance",
        "reports",
        "deps",
        "release"
      ]
    ],
    "subject-case": [0]
  }
}
